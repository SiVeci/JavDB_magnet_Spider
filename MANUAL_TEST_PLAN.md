# JavDB Magnet Spider — 手动测试计划（Phase 0~4 重构后回归）

> 目的：在 Phase 0~4 重构后，验证 PC / Docker / Android 三端全功能正常，UI 视觉与操作逻辑不变。
> 标注 **[回归]** 的用例是本次重构的高风险点，务必优先执行。
> 结果列填写：✅通过 / ❌失败（附现象）/ ⏭️跳过。

---

## 0. 测试环境准备

| # | 测试动作 | 预期效果 |
|---|---|---|
| 0.1 | PC 端：`pip install -r requirements.txt`，确认 `curl_cffi` 可正常 `import`（之前报过 TLS 库冲突，必要时 `pip install -U curl_cffi`） | 无 `OPENSSL_internal:invalid library` 报错 |
| 0.2 | PC 端启动：`cd spider_core && python -m uvicorn main:app --host 0.0.0.0 --port 8000` | 控制台打印 `Application startup complete`，无 traceback |
| 0.3 | Docker 端：按项目 Dockerfile 构建并运行容器，映射 8000 端口 | 容器正常启动，日志无异常 |
| 0.4 | Android 端：安装重构后 APK（确认 `local.properties` 含签名凭据后打包） | 安装成功，可正常打开 App |
| 0.5 | 浏览器访问 `http://127.0.0.1:8000` | 页面正常加载（见模块一） |

---

## 一、前端加载与静态资源 **[回归：Phase 1 前端拆分 + StaticFiles 挂载]**

> 重点：index.html 从单文件拆成 4 CSS + 8 JS，由 FastAPI StaticFiles 挂载。

| # | 测试动作 | 预期效果 | 结果 |
|---|---|---|---|
| 1.1 | 打开首页，观察整体布局/配色/字体 | 与重构前**视觉完全一致**：浅灰背景、卡片式分区、顶部 Logo+标题+版本号徽标 | |
| 1.2 | 浏览器 F12 → Network，刷新页面 | `/css/variables.css`、`base.css`、`components.css`、`pages.css` 与 `/js/utils.js`…`/js/app.js`（共 8 个）均 **200** 加载，无 404 | |
| 1.3 | F12 → Console | **无 JavaScript 报错**（尤其无 `xxx is not defined`，验证全局函数接线正确） | |
| 1.4 | 任务列表/日志区滚动 | 滚动条为自定义细灰色样式（验证 `base.css` 的 `.log-box` 生效） | |
| 1.5 | favicon | 浏览器标签页显示站点图标（`/favicon.png` 正常） | |

---

## 二、鉴权（仅当设置了访问令牌时）**[回归：中间件 + 静态资源放行]**

> 设置 `JAVDB_AUTH_TOKEN=xxx` 环境变量后重启再测；未设置则整模块跳过。

| # | 测试动作 | 预期效果 | 结果 |
|---|---|---|---|
| 2.1 | 启用令牌后访问首页 | 显示**深色鉴权遮罩**（输入访问令牌界面），主界面隐藏 | |
| 2.2 | 输入错误令牌点「验证」 | 提示「访问令牌缺失或无效」，不放行 | |
| 2.3 | 输入正确令牌（或回车） | 遮罩消失，主界面加载，数据正常拉取 | |
| 2.4 | 鉴权状态下，F12 Network 确认 css/js 加载 | `/css/*`、`/js/*` **无需令牌即 200**（它们不在 `/api/` 下，应放行） | |
| 2.5 | 刷新页面 | 因 sessionStorage 保存令牌，自动放行无需重输 | |
| 2.6 | 未启用令牌时访问 | 直接进入主界面，无鉴权遮罩 | |

---

## 三、任务配置与标签

| # | 测试动作 | 预期效果 | 结果 |
|---|---|---|---|
| 3.1 | 「全局运行配置」面板填写 Cookie / User-Agent / 代理 / Tracker，点「保存全局配置」 | 顶部出现 **Toast 提示**「运行配置已保存」（**非阻塞 alert**，验证 Phase 1 Toast 改造） | |
| 3.2 | 勾选「记住 Cookie」后保存，刷新页面 | Cookie 仍在（写入后端 DB） | |
| 3.3 | 代理只填 host 不填 port 点保存 | Toast 提示「代理地址和端口必须同时填写」 | |
| 3.4 | 起始 URL 填演员页，点「标签」按钮 | 拉取到标签列表，展开标签过滤面板 | |
| 3.5 | **[回归]** 网络不通/TLS 失败时点「标签」 | Toast 提示「标签页请求失败：…」**（502 友好提示，后端不崩 traceback）** | |
| 3.6 | 点选若干标签 | 起始 URL 自动拼接 `t=` 参数，标签高亮 | |
| 3.7 | 折叠/展开「全局运行配置」「标签过滤」面板 | 面板正常收起/展开，箭头图标翻转 | |

---

## 四、任务队列与监控 **[回归：清理遗留 API + setTimeout 轮询]**

> 重构删除了 `/api/start`、`/api/stop`、`/api/resume`、`/api/set_mode`，现统一走 `/api/tasks/*`。

| # | 测试动作 | 预期效果 | 结果 |
|---|---|---|---|
| 4.1 | 填写起始 URL，点「添加到任务列表」 | 任务出现在任务列表，状态「排队中」 | |
| 4.2 | 对同名已存在集合添加任务 | 弹确认框，选增量/覆盖；选择后任务正确入队 | |
| 4.3 | **[回归]** 网络/TLS 失败时添加任务 | Toast 提示「入队预检查请求失败：…」**（502，后端命令行无整页 traceback）** | |
| 4.4 | 点「开始任务队列」 | 队列状态变「运行中」，任务转「运行中」，进度条与日志实时刷新 | |
| 4.5 | **[回归]** 让任务运行数分钟，观察轮询 | 监控面板每 ~2.5s 刷新；**断网再恢复后轮询能继续，不会卡死或请求堆积**（验证 setInterval→setTimeout 链改造） | |
| 4.6 | 任务运行中点「暂停」 | 任务转「暂停中→已暂停」，进度保存 | |
| 4.7 | 暂停后点「恢复」 | 任务重新入队并运行 | |
| 4.8 | 运行中点「取消」 | 弹确认框，确认后任务转「已取消」 | |
| 4.9 | 点「删除」任务记录 | 弹确认框，确认后任务从列表移除（不删数据库集合） | |
| 4.10 | 点「显示已结束 / 隐藏已结束」 | 已完成/取消/失败任务正确显隐 | |
| 4.11 | 点「清理已结束」 | 弹确认框，确认后清空已结束任务，Toast 提示数量 | |
| 4.12 | 「运行日志」面板折叠/展开 | 日志区正常收起/展开 | |
| 4.13 | Cookie 失效导致任务挂起（waiting_cookie） | 任务显示「等待 Cookie」，出现「恢复 / 读安卓 Cookie」按钮 | |

---

## 五、数据库管理 — 集合列表

| # | 测试动作 | 预期效果 | 结果 |
|---|---|---|---|
| 5.1 | 完成一次爬取后看「数据库管理」表格 | 集合按更新时间倒序，显示名称/时间/标签数/影片数 | |
| 5.2 | 点集合行展开箭头 | 展开影片列表，箭头旋转 180° | |
| 5.3 | 同时展开另一集合 | 前一个自动收起（单展开逻辑） | |
| 5.4 | 点「刷新」按钮（右上圆形箭头） | 集合列表重新拉取 | |
| 5.5 | 勾选多个集合 → 出现批量删除红色按钮，角标显示数量 | 角标数字与勾选数一致 | |
| 5.6 | 点「全选」复选框 | 所有集合复选框全选/全不选联动 | |
| 5.7 | 点批量删除（垃圾桶图标） | 弹确认框，确认后删除选中集合 | |
| 5.8 | 点单集合「×」删除 | 弹确认框，确认后删除该集合 | |
| 5.9 | 删除正在被运行任务占用的集合 | 提示「被占用」，删除失败但其余成功 | |

---

## 六、影片浏览与标签过滤

| # | 测试动作 | 预期效果 | 结果 |
|---|---|---|---|
| 6.1 | 展开集合，查看影片卡片 | 显示番号、标题、候选磁力数、已选磁力名、标签 | |
| 6.2 | 影片标签过多时观察 | 标签自适应折叠为「+N」（`fitMovieTags`） | |
| 6.3 | 点「筛选」下拉，勾选某标签 | 影片列表按标签过滤，计数「筛选: X/Y」更新 | |
| 6.4 | **[回归]** 点「排除」下拉，勾选某标签 | 含该标签的影片被排除（验证 exclude_tags 过滤） | |
| 6.5 | 点「清除排除」 | 排除条件清空，影片全显示 | |
| 6.6 | 下拉打开时点页面空白处 | 下拉自动关闭（`handleDocumentClick`） | |
| 6.7 | 点影片展开箭头看候选磁力 | 展开磁力表格：状态/文件名/分数/大小/操作 | |
| 6.8 | 点某候选磁力的圆圈（设为选中） | 该磁力标记选中（绿勾），影片「已选磁力名」更新 | |
| 6.9 | 点磁力行「复制」图标 | 磁力链接复制到剪贴板（Toast 确认） | |

---

## 七、磁力验活 **[回归：Phase 2-2 合并三端点 + magnet_service 抽取]**

> 三个检查端点（movie/collection/all）合并为共享 helper，且检测编排移到 magnet_service。

| # | 测试动作 | 预期效果 | 结果 |
|---|---|---|---|
| 7.1 | 单影片点「check」（雷达图标） | 启动检测，按钮转圈显示进度，完成后磁力状态图标更新（🟢有效/🟡弱/🔴无效/❌失败/⚪未检测） | |
| 7.2 | 集合级点「check」 | 批量检测该集合所有影片磁力，进度实时刷新 | |
| 7.3 | 全局（数据库管理右上）点「check」 | 检测**所有集合**全部磁力 | |
| 7.4 | **[回归]** 检测进行中再点其它「check」 | 提示「磁力检测任务正在运行」（409 并发冲突，返回当前运行中任务） | |
| 7.5 | 点检测按钮旁下拉「check failed」 | 仅重检之前检测失败的磁力 | |
| 7.6 | 检测中点「取消检测」 | 任务停止，状态「检测已取消」 | |
| 7.7 | 检测完成后看集合健康度统计（2×2 格） | 有效/弱/无效/失败影片数正确 | |
| 7.8 | **[回归]** 检测中刷新浏览器页面 | 重新进入后能恢复显示正在运行的检测任务（`/magnet_check_jobs/current` 恢复） | |
| 7.9 | 检测完成后，最优磁力被自动选为「已选」 | 影片的 best_magnet 按评分/有效性自动更新 | |

---

## 八、导出与复制 **[回归：Phase 2-4 export_service 拆分 + exclude_tags]**

| # | 测试动作 | 预期效果 | 结果 |
|---|---|---|---|
| 8.1 | 集合「复制磁力」（⧉图标） | 复制该集合所有已选磁力链接，Toast 提示条数 | |
| 8.2 | **[回归]** 设置标签过滤后再「复制磁力」 | 仅复制符合过滤条件的磁力（tags/exclude_tags 生效） | |
| 8.3 | 集合「下载 CSV」（⇩图标） | 下载 CSV 文件，文件名为集合名，含 BOM 头，中文不乱码 | |
| 8.4 | 打开下载的 CSV | 列含番号/标题/链接/最佳资源名/磁力/分数/日期/大小，数据正确 | |
| 8.5 | **[回归]** 带 exclude_tags 下载 CSV | 被排除标签的影片不出现在 CSV 中 | |
| 8.6 | 增量任务完成后点任务的「复制新增影片磁力」（⧉） | 仅复制本次增量新增影片的磁力 | |
| 8.7 | 点「★」自动选择磁力（全部或选中集合） | 弹确认框，确认后按评分自动选最优磁力，Toast 提示影片数 | |
| 8.8 | 集合行「⟳」快捷增量（有原始 URL 时） | 弹确认框，确认后该集合增量任务入队 | |

---

## 九、错误码与边界 **[回归：Phase 2-3 响应模式统一]**

> 重构把 HTTP 200+body code 改为 HTTP 状态码与 body code 对齐。可用浏览器 F12 Network 或 curl 验证。

| # | 测试动作 | 预期效果 | 结果 |
|---|---|---|---|
| 9.1 | 访问不存在集合 `/api/collections/nope.csv/movies` | HTTP **404**，body `{"code":404,...}` | |
| 9.2 | `/api/magnets`（不带 name 参数） | HTTP **400**，body `{"code":400,...}` | |
| 9.3 | `/api/download?name=nope.csv` | HTTP **404** | |
| 9.4 | 暂停不存在的任务 `/api/tasks/xxx/pause` | HTTP **400**，提示「任务不存在或状态不支持暂停」 | |
| 9.5 | 已删除的遗留端点 `/api/start`、`/api/stop`、`/api/resume`、`/api/set_mode` | 均返回 **404 Not Found**（已被删除，不应再存在） | |
| 9.6 | 路径遍历文件名 `/api/download?name=../etc/passwd` | HTTP **400**「文件名非法」 | |

---

## 十、Docker 端专项

| # | 测试动作 | 预期效果 | 结果 |
|---|---|---|---|
| 10.1 | 容器启动后访问 `http://<host>:8000` | 前端正常加载（css/js 在容器内打包正确） | |
| 10.2 | 容器内完整跑一次爬取+导出 | 功能与 PC 端一致 | |
| 10.3 | 重启容器后访问 | 历史集合数据持久化（数据卷挂载正确） | |
| 10.4 | 检查容器日志 | 启动与运行无 traceback，轮询访问日志被静默（`_QuietPollFilter`） | |

---

## 十一、Android 端 **[回归：Phase 4 全部改动]**

> 本端改动无法在 PC 验证，全部依赖真机。重点：WebView 生命周期、通知后台线程、Python 崩溃提示、返回键。

| # | 测试动作 | 预期效果 | 结果 |
|---|---|---|---|
| 11.1 | 打开 App，看主界面 | 控制面板显示，引擎状态默认未运行 | |
| 11.2 | 点「登录」 | 进入登录 WebView，加载 javdb.com/login | |
| 11.3 | **[回归 4-D]** 登录页按手机**返回键** | 回到控制台界面，**不退出 App**（WebView 加载 about:blank） | |
| 11.4 | 控制台界面再按返回键 | 正常退出 App（默认行为） | |
| 11.5 | 登录后点「关闭登录」 | 回控制台，Toast「Cookie 已自动接管」 | |
| 11.6 | 点「启动引擎」 | 依次申请通知/悬浮窗权限，授权后启动服务，状态变🟢运行中，Toast 提示 | |
| 11.7 | 启动后下拉通知栏 | 显示「JavDB 爬虫引擎运行中」前台通知 | |
| 11.8 | **[回归 4-B]** 在 App 内发起爬取，观察通知栏 | 通知标题/进度每 ~2s 更新（▶️爬取中…），**App 操作流畅不卡顿/无 ANR**（状态读取已移后台线程） | |
| 11.9 | 点「打开浏览器」 | 唤起系统浏览器访问 `127.0.0.1:8000`，可操作控制台 | |
| 11.10 | 在 App 内 WebView 完成 javdb 登录，发起爬取 | 走原生 WebView 抓取（不依赖 curl_cffi，绕过 PC 端 TLS 问题），任务正常运行 | |
| 11.11 | 任务挂起等 Cookie 时点「读安卓 Cookie」 | 自动读取 WebView 当前 Cookie 并恢复任务 | |
| 11.12 | **[回归 4-A]** 反复横竖屏旋转 / 退到后台再回前台 | 界面正常，WebView 不崩溃、无明显内存增长（生命周期管理生效） | |
| 11.13 | **[回归 4-C]** 制造 Python 后端崩溃（如断依赖/异常场景） | 通知栏出现「❌ 引擎已停止」提示，引擎状态不再是僵尸；回 App 可重新启动 | |
| 11.14 | 锁屏息屏后台爬取一段时间 | 借助悬浮窗+前台服务持续运行，回前台进度正常 | |
| 11.15 | 「复制当前链接」按钮 | 复制 WebView 当前 URL 到剪贴板，Toast 确认 | |

---

## 十二、跨端一致性与收尾

| # | 测试动作 | 预期效果 | 结果 |
|---|---|---|---|
| 12.1 | 同一任务在 PC 创建、Android 查看（同后端） | 任务/集合数据一致 | |
| 12.2 | 长时间运行（多任务排队） | 队列依次执行，无内存泄漏/僵尸线程 | |
| 12.3 | 全程对比重构前后 UI 截图 | **视觉无差异**（验收硬指标：UI 不变） | |
| 12.4 | 全程对比重构前后操作流程 | **操作逻辑无差异**（验收硬指标：交互不变） | |

---

## 高优先级回归清单（时间有限时至少跑这些）

1. **1.2 / 1.3** — css/js 全部 200 加载、Console 无报错（前端拆分根基）
2. **4.5** — 断网恢复后轮询继续（setTimeout 链改造）
3. **3.5 / 4.3** — 网络失败返回 502 友好提示而非 traceback（异常收口）
4. **7.4 / 7.8** — 磁力检测并发冲突 + 刷新恢复（magnet_service 抽取）
5. **8.2 / 8.5** — tags/exclude_tags 过滤在复制与下载中生效
6. **9.5** — 遗留端点已彻底删除（404）
7. **11.3 / 11.8 / 11.13** — Android 返回键、通知不卡顿、崩溃提示（Phase 4）
8. **12.3 / 12.4** — UI 视觉与操作逻辑无变化（验收硬指标）
